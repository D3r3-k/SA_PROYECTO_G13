version: "3.8"

services:
  web:
    build:
      context: ../apps/web
      dockerfile: Dockerfile
    container_name: sa_web
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
    networks:
      - sa_net

networks:
  sa_net:
    name: sa_net
    external: true
