apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: quetxal-tv-prod
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: grpc
      port: 50054
      targetPort: 50054
