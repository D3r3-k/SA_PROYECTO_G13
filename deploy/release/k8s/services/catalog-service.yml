apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: quetxal-tv-prod
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - name: grpc
      port: 50055
      targetPort: 50055
