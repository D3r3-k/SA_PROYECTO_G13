apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: quetxal-tv-prod
spec:
  type: ClusterIP
  selector:
    app: subscription-service
  ports:
    - name: grpc
      port: 50053
      targetPort: 50053
