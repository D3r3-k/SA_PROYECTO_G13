apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: quetxal-tv-prod
  annotations:
    cloud.google.com/backend-config: '{"ports":{"80":"web-backend-config"}}'
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
