apiVersion: v1
kind: Service
metadata:
  name: acme-solver-service
  namespace: quetxal-tv-prod
spec:
  ports:
    - port: 8089
      targetPort: 8089
      protocol: TCP
  selector:
    acme.cert-manager.io/http01-solver: "true"
