# Release Kubernetes

Directorio reservado para la configuracion futura de produccion en Kubernetes.

Estructura sugerida:

```text
k8s/
  namespace.yml
  configmap.yml
  secrets.example.yml
  deployments/
  services/
  ingress.yml
```
