apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: quetxal-tv-ip
  namespace: quetxal-tv-prod
spec:
  secretName: quetxal-tv-tls
  issuerRef:
    name: letsencrypt-__ACME_ENVIRONMENT__
    kind: ClusterIssuer
  ipAddresses:
    - __INGRESS_IP__
