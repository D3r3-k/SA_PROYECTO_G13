# Optional SMTP configuration. If set, notifications will be sent via SMTP.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=notifications@example.com

# Database (if used)
DATABASE_URL=postgresql://sa_user:sa_pass@postgres:5432/sa_db