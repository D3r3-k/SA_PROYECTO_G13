syntax = "proto3";
package subscription;

service Subscription {
  rpc ListPlans (Empty) returns (PlanList) {}
}

message Empty {}

message Plan {
  string id = 1;
  string name = 2;
  double price_usd = 3;
}

message PlanList {
  repeated Plan plans = 1;
}
