syntax = "proto3";
package fx;

service FX {
  rpc GetRate (RateRequest) returns (RateResponse) {}
}

message RateRequest {
  string from = 1;
  string to = 2;
}

message RateResponse {
  double rate = 1;
  int64 timestamp = 2;
}
